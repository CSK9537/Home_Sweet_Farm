<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.joonzis.community.mapper.CommunityFormMapper">

  <select id="selectBoardSeqNextVal" resultType="int">
    SELECT BOARD_SEQ.NEXTVAL FROM DUAL
  </select>

  <insert id="insertBoard" parameterType="org.joonzis.community.vo.BoardVO">
    INSERT INTO TBL_BOARD (
      BOARD_ID,
      USER_ID,
      CATEGORY_ID,
      PARENT_ID,
      TITLE,
      CONTENT,
      BOARD_TYPE,
      PRICE,
      TRADE_STATUS,
      VIEW_COUNT,
      REPLY_CNT,
      LIKE_COUNT,
      IS_ACTIVE,
      IS_SELECTED,
      REG_DATE,
      UPDATE_DATE
    ) VALUES (
      #{board_id},
      #{user_id},
      #{category_id, jdbcType=NUMERIC},
      #{parent_id, jdbcType=NUMERIC},
      #{title},
      #{content},
      #{board_type},
      #{price, jdbcType=NUMERIC},
      #{trade_status, jdbcType=VARCHAR},
      0,
      0,
      0,
      'Y',
      'N',
      SYSDATE,
      SYSDATE
    )
  </insert>

  <update id="updateBoard" parameterType="org.joonzis.community.vo.BoardVO">
    UPDATE TBL_BOARD
       SET CATEGORY_ID  = #{category_id, jdbcType=NUMERIC},
           PARENT_ID    = #{parent_id, jdbcType=NUMERIC},
           TITLE        = #{title},
           CONTENT      = #{content},
           PRICE        = #{price, jdbcType=NUMERIC},
           TRADE_STATUS = #{trade_status, jdbcType=VARCHAR},
           UPDATE_DATE  = SYSDATE
     WHERE BOARD_ID     = #{board_id}
       AND IS_ACTIVE    = 'Y'
  </update>

  <select id="selectBoardById"
          resultType="org.joonzis.community.vo.BoardVO">
    SELECT
      BOARD_ID,
      USER_ID,
      CATEGORY_ID,
      PARENT_ID,
      TITLE,
      CONTENT,
      BOARD_TYPE,
      PRICE,
      TRADE_STATUS,
      VIEW_COUNT,
      REPLY_CNT,
      LIKE_COUNT,
      IS_ACTIVE,
      IS_SELECTED,
      REG_DATE,
      UPDATE_DATE
    FROM TBL_BOARD
    WHERE BOARD_ID  = #{boardId}
      AND IS_ACTIVE = 'Y'
  </select>

  <select id="selectBoardOwnerUserId"
          resultType="int">
    SELECT USER_ID
    FROM TBL_BOARD
    WHERE BOARD_ID = #{boardId}
      AND IS_ACTIVE = 'Y'
  </select>

  <insert id="insertBoardFile"
          parameterType="org.joonzis.community.vo.BoardFileVO">
    INSERT INTO TBL_BOARD_FILE (
      FILE_ID,
      BOARD_ID,
      ORIGINAL_NAME,
      SAVED_NAME,
      FILE_SIZE,
      CONTENT_TYPE,
      TEMP_KEY,
      SUB_DIR,
      FILE_KIND,
      IS_ACTIVE,
      IS_THUMBNAIL,
      REG_DATE,
      UPDATED_AT
    ) VALUES (
      BOARD_FILE_SEQ.NEXTVAL,
      #{board_id, jdbcType=NUMERIC},
      #{original_name},
      #{saved_name},
      #{file_size, jdbcType=NUMERIC},
      #{content_type},
      #{temp_key, jdbcType=VARCHAR},
      #{sub_dir},
      #{file_kind},
      #{is_active},
      #{is_thumbnail},
      SYSDATE,
      SYSDATE
    )
  </insert>

  <select id="selectFilesByTempKey"
          parameterType="string"
          resultType="org.joonzis.community.vo.BoardFileVO">
    SELECT
      FILE_ID,
      BOARD_ID,
      ORIGINAL_NAME,
      SAVED_NAME,
      FILE_SIZE,
      CONTENT_TYPE,
      TEMP_KEY,
      SUB_DIR,
      FILE_KIND,
      IS_ACTIVE,
      IS_THUMBNAIL,
      REG_DATE,
      UPDATED_AT
    FROM TBL_BOARD_FILE
    WHERE TEMP_KEY  = #{temp_key, jdbcType=VARCHAR}
      AND IS_ACTIVE = 'Y'
    ORDER BY FILE_ID ASC
  </select>

  <update id="attachTempFilesToBoard">
    UPDATE TBL_BOARD_FILE
       SET BOARD_ID   = #{board_id, jdbcType=NUMERIC},
           TEMP_KEY   = NULL,
           UPDATED_AT = SYSDATE
     WHERE TEMP_KEY   = #{temp_key, jdbcType=VARCHAR}
       AND BOARD_ID   IS NULL
       AND IS_ACTIVE  = 'Y'
  </update>

  <update id="deactivateFilesByBoardId">
    UPDATE TBL_BOARD_FILE
       SET IS_ACTIVE  = 'N',
           UPDATED_AT = SYSDATE
     WHERE BOARD_ID   = #{boardId}
       AND IS_ACTIVE  = 'Y'
  </update>

  <update id="deactivateRepliesByBoardId">
    UPDATE TBL_REPLY
       SET IS_ACTIVE  = 'N',
           UPDATED_AT = SYSDATE
     WHERE BOARD_ID   = #{boardId}
       AND IS_ACTIVE  = 'Y'
  </update>

  <update id="deactivateBoard">
    UPDATE TBL_BOARD
       SET IS_ACTIVE   = 'N',
           UPDATE_DATE = SYSDATE
     WHERE BOARD_ID    = #{boardId}
       AND USER_ID     = #{loginUserId}
       AND IS_ACTIVE   = 'Y'
  </update>

  <select id="selectHashtagSuggest" resultType="string">
    SELECT BOARD_HASHTAG_NAME
    FROM (
      SELECT BOARD_HASHTAG_NAME
      FROM TBL_BOARD_HASHTAG_LIST
      WHERE IS_ACTIVE = 'Y'
        AND LOWER(BOARD_HASHTAG_NAME) LIKE LOWER(#{q}) || '%'
      ORDER BY BOARD_HASHTAG_NAME
    )
    WHERE ROWNUM &lt;= #{limit}
  </select>

</mapper>