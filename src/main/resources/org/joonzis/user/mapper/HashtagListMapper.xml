<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.joonzis.user.mapper.HashtagListMapper">
	<!--<select id="searchNames" resultType="String">
		SELECT hashtag_name
		FROM tbl_hashtag_list
		WHERE hashtag_id = #{hashtag_id}
	</select>
	
	<select id="">
		SELECT hashtag_id
		FROM tbl_hashtag_list
		WHE
	</select>  -->
	
	
</mapper>