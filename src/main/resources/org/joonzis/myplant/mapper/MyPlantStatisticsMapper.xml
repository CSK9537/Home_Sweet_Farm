<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.joonzis.myplant.mapper.MyPlantStatisticsMapper">
	<select id="findSensorDataByMyplantId" resultType="org.joonzis.myplant.dto.MyPlantStatisticsDTO">
		SELECT 
			myplant_id,
			temperature,
			humidity,
			illumination,
			soil_moisture,
			sensing_time
		FROM tbl_myplant_statistics
		WHERE myplant_id = #{myplant_id}
		ORDER BY sensing_time ASC
	</select>
	
</mapper>